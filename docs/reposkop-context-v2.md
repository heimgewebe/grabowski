# Reposkop context tool v2 — retired compatibility tombstone

This path is retained only so historical references, including `reposkop-context-v1.md`, resolve to an explicit current disposition.

As of September 2026, Grabowski no longer has an active Reposkop consumer, public Reposkop tool, admission requirement, shadow/effectiveness pipeline, or Reposkop runtime dependency. Repository state is read from the native typed Git, checkout, lease/lifecycle, and GitHub surfaces instead.

The previous v2 contract remains available in Git history. This document grants no compatibility, execution, migration, or recovery authority.
